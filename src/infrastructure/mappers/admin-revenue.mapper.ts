import { Transaction, WalletTransaction } from "@prisma/client";

export class AdminRevenueMapper {
    static toDomains(stripeTransaction:Transaction[],walletTransaction:WalletTransaction[]){

    }
    static toDomain(stripe:Transaction,wallet:WalletTransaction) {
        
    }
}